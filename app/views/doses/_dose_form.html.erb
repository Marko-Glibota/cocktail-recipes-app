<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :ingredient_id, :collection => @ingredients, required: true %>
  <%= f.input :description %>
  <%= f.submit "Add ingredient", class: 'button-red' %>
<% end %>
